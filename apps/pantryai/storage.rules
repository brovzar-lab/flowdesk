rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Ephemeral scan uploads: users can write to their own scan path;
    // Cloud Functions service account handles cleanup after processing.
    match /scans/{uid}/{scanId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
